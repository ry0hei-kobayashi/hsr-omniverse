# Hamburger

`model.usd` is the Isaac Sim entry point. It references the unpacked
`source.usdc` from `Hamburger.usdz`, preserves its bundled PBR textures, converts
Y-up to Z-up, and converts the source centimetres to metres.

Expected composed size: approximately 0.121 x 0.118 x 0.094 m.
